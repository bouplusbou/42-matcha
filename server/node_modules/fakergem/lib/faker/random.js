'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _random = require('../utils/random');

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function shuffle(collection) {
  var index = -1;
  var length = collection.length;
  var result = Array(length);
  while (++index < length) {
    var rand = (0, _random.randomNumber)(0, index);
    result[index] = result[rand];
    result[rand] = collection[index];
  }
  return result;
}

var Random = function () {
  function Random(faker) {
    _classCallCheck(this, Random);

    this.faker = faker;
  }

  _createClass(Random, [{
    key: 'assortment',
    value: function assortment(array, n) {
      var count = Math.max(0, n);
      var repeatCount = parseInt(count / array.length) || 1;
      var repeated = Array.apply(null, { length: repeatCount * array.length }).map(function (e, i) {
        return array[i % array.length];
      });
      return shuffle(repeated).slice(0, count);
    }
  }, {
    key: 'element',
    value: function element(array) {
      if (!array) {
        throw new Error('An array or string must be specified');
      }
      return (0, _random.itemFromCollection)(array);
    }
  }]);

  return Random;
}();

exports.default = Random;